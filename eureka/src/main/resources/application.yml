server:
  port: 8761
  address: 0.0.0.0
spring:
  application:
    name: Eureka_Server

eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false
  instance:
    hostname: localhost
    lease-renewal-interval-in-seconds: 1
    lease-expiration-duration-in-seconds: 1